# CLAUDE.md

Claude Code reads `CLAUDE.md`; the canonical, cross-agent rules live in `AGENTS.md`
(read natively by Codex/Amp/Cursor/Gemini). This shim imports them so a session started at
the repo root has the full ruleset loaded.

@AGENTS.md
